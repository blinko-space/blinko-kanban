# Blinko Kanban

Blinko Kanban is a sidebar App for organizing tasks across flexible boards and columns.

Each board is stored as a versioned App-owned entity. Board titles and card copy join Blinko keyword search, while all writes preserve optimistic concurrency.

## Development

```bash
bun install
bun run validate
bun run typecheck
bun test
bun run build
```
